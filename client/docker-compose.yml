services:
  frontend:
    build:
      context: ..
      dockerfile: client/Dockerfile
    container_name: gravity-client
    restart: unless-stopped
    ports:
      - "33101:80"
    environment:
      - BACKEND_UPSTREAM=http://host.docker.internal:38080
    extra_hosts:
      - "host.docker.internal:host-gateway"
