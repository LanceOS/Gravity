// Includes room for a two-line title, PR badge, and metadata at the board's narrowest width.
export const TICKET_CARD_HEIGHT = 160;
// Match the 8px gap used by the Kanban board's non-virtual card stack.
export const TICKET_CARD_ROW_HEIGHT = TICKET_CARD_HEIGHT + 8;
