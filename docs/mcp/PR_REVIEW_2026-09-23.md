# MCP follow-up PR review — 2026-09-23

The initial review covered the then-uncommitted working tree on `codex/mcp-reliability-and-granularity`, including new files and regression tests. The subsequent review of published [PR #217](https://github.com/LanceOS/Gravity/pull/217) is recorded below.

## Findings fixed

| Finding | Resulting behavior |
| --- | --- |
| Parent detail updates were rejected or lost in realtime processing. | Changing, clearing, or moving a child advances the affected parents' timestamps under the project lock. The client preserves explicit `subtasks` snapshots, including an empty array; flat updates preserve the cached expansion. A regression exercises actual client caches with same-user events. |
| Label replacement reported success after a concurrent ticket move prevented the write. | `set_ticket_labels` checks the service result and returns a retryable conflict instead of publishing a misleading success event. A gated concurrent-move regression verifies the destination labels survive. |
| Moving a project to another team left incompatible cycles attached, causing subsequent ticket edits to fail validation. | Project team reassignment locks the project and clears incompatible ticket cycles in the same transaction. Destination-team cycles and tickets in other projects are preserved. Tests cover destinations with and without cycles, followed by a ticket edit. |
| Browser WebMCP registration could reject asynchronously after the hook considered it complete. | Registration exposes a completion promise and an immediately available disposal function. Failures clean up prior registrations and reach the hook's error handler; disposal can cancel pending registration. |
| The standalone server lockfile did not include the new server dependencies. | `server/package-lock.json` is synchronized for the server-only install used by the container build. Final container-build verification is tracked below. |
| Legacy HMAC secrets containing `:` or `=` could be mistaken solely for keyed-secret entries. | Rotation verification retains the complete configured value as a legacy candidate, including padded secrets, while explicitly keyed tokens still require the matching key entry. |
| Standalone stdio shutdown could leave database or Redis connections open. | EOF and signal shutdown release the event bridge, shared Redis client, and database pool, and remove signal listeners. |
| Redis mutation forwarding used a channel shared across logical databases and deployments. | The channel includes the Redis database number and optional `MCP_EVENT_NAMESPACE`, preventing unrelated configured namespaces from sharing mutation messages. HTTP and stdio processes belonging to one deployment must use matching configuration. |
| An existing branch-slug test failed because underscores were removed before separator normalization. | Client and server branch-name utilities now turn underscores into separators consistently; this small baseline fix is included in the working tree. |
| A restored legacy database still required `cycles.project_id`, blocking creation of modern team-owned cycles. | Bootstrap relaxes the obsolete constraint after backfilling team ownership, preserving existing cycle records and project references. A legacy-schema regression verifies repeated migration and modern cycle insertion; current schemas remain unchanged. |
| Revoking ten old credentials exhausted the shared issuance quota and blocked Generate Connection. | Revocation and creation/refresh have separate named counters while retaining their existing caps. Both limiter implementations return retry delays, and the dialog displays them without discarding an existing configuration. Route regressions cover generation after ten revocations and revocation after issuance throttling. |

## Validation recorded

- Earlier full client suite: **667 passed** across 107 suites (`NODE_OPTIONS=--no-experimental-webstorage npm run -w client test`). Subsequent modal improvements (Select All, library dropdown, success feedback, and rate-limit handling) pass all **9 focused modal tests** and client TypeScript compilation.
- Full server suite: **364 passed** across 54 suites (`npm run -w server test`), including official MCP SDK HTTP and stdio interoperability, legacy cycle migration, and independent connection quotas.
- MCP SSE integration suite: **5 passed** (`NODE_OPTIONS=--no-experimental-webstorage npm run -w client test:e2e -- mcp-sse-pipeline`).
- Targeted server suites: **34 passed** — `mcp-domain-safety.test.ts` (21), `teams.test.ts` (4), and `projects-tickets.test.ts` (9).
- Server build, client TypeScript compilation, and `git diff --check`: passed.
- Targeted server log: `/tmp/gravity-pr-domain-adjacent-tests.log`.

The server route tests require local listener access. The first full run during a concurrent Docker build exceeded the shutdown fixture's five-second startup deadline. The fixture now allows twenty seconds to start, reports early child exits, and retains its two-second shutdown deadline. The final full run passed without failures.

## Final deployment verification

- Production-style Docker image build: passed for frontend and backend. The logs confirm both real Vite bundles and the server TypeScript build succeeded; the Dockerfiles' fallback outputs were not used.
- Rebuilt the existing `gravity-test-prod` frontend/backend images and recreated only those two application containers with `up -d --no-deps --force-recreate --wait`. Both are healthy. PostgreSQL, Redis, RustFS, and their volumes were retained.
- [Test application](http://localhost:9999): production health endpoint returns `status: ok`; the browser renders the sign-in page with the final assets.
- Live MCP smoke passed against real PostgreSQL and Redis: initialization and notification handling, all **38 canonical tools**, scoped project discovery, ticket creation/read/priority changes, parent clearing and parent snapshots, project team movement with cycle cleanup, bounded search, malformed-argument rejection, generic-update field policy enforcement, credential revocation, and event delivery from the separate HTTP process.
- The final smoke inserts modern team-owned cycles without `project_id`, proving the legacy constraint migration ran on the restored database. All uniquely scoped test records were removed afterward. Row counts for **26 tables** match the pre-restart snapshot, and an aggregate checksum confirms existing cycle rows are unchanged.
- Live Redis isolation checks passed for matching processes, different logical databases, and different deployment namespaces. Compiled stdio processes with real Redis exited cleanly on EOF, SIGTERM, and SIGINT without forced termination.
- Live connection-quota regression passed after redeployment: ten older credentials were revoked, an eleventh revocation returned 429, and Generate Connection still returned 201 with a credential the official SDK used to read the workspace. Ten combined creations/refreshes were accepted; the eleventh returned 429 with matching `Retry-After` and JSON delays. The disposable account, workspace, credentials, and session were cleaned up; existing rate-limit counters were not cleared. Evidence: `/tmp/gravity-mcp-revoke-issue-quota.log` and `/tmp/gravity-mcp-rate-limits-full-tests.log`.

No remaining blocking findings were identified in the reviewed scope. Live external-model calls, supported-browser WebMCP execution, real PostgreSQL concurrency, and a full multiple-HTTP-replica deployment remain outside this verification. WebMCP and provider regressions use mocked browser/provider boundaries.

Local evidence: `/tmp/gravity-mcp-pr-server-tests-final.log`, `/tmp/gravity-mcp-pr-client-tests.log`, `/tmp/gravity-mcp-pr-sse-tests.log`, `/tmp/gravity-mcp-pr-final-build.log`, `/tmp/gravity-mcp-pr-migration-build.log`, `/tmp/gravity-mcp-pr-restart-final.log`, and `/tmp/gravity-mcp-pr-live-smoke-final.log`.

At the end of that initial review, the changes remained uncommitted and no PR had been published. They were subsequently committed as `c3655c37` and published in PR #217.

## Account settings and OAuth follow-up

Connection management moved from the workspace sidebar to **Account Preferences → Connect External AI**. The account inventory reads only the current user's credentials and current workspace memberships, groups by workspace, and exposes permissions, expiry, client labels, and revocation. New connections offer copyable fields/JSON for custom-header clients and a separate ChatGPT OAuth setup view. A failed inventory refresh cannot close the generated credential view.

OAuth uses the official MCP SDK with a persistent Gravity provider: public-client dynamic registration, authorization-code PKCE S256, explicit browser-session consent, workspace resource binding, one-hour access tokens, and rotating refresh tokens bounded by a 30-day grant. Existing role, workspace policy, and execution checks remain in force. Consent defaults to read tools and supports explicit bulk selection and denial.

Review corrections include:

- Initial unauthenticated OAuth requests reach a 401 discovery challenge under production CSRF protection. The exemption applies only to the OAuth transport, which never authorizes browser cookies; legacy MCP and consent retain CSRF checks.
- Manual refresh cannot convert an OAuth grant into a custom-header credential or change its lifetime. Manual creation cannot claim the reserved OAuth connection type.
- Grant and refresh transactions use their existing database connection for membership/policy reads, avoiding pool-exhaustion deadlocks.
- Unsupported HTTP issuer origins disable OAuth with a clear explanation instead of crashing the application. Manual connections still work. The local `localhost:9999` deployment remains supported.
- OAuth metadata/auth endpoints are proxied by nginx and Vite, and OAuth tool descriptors advertise exact security scopes.

Actual ChatGPT verification is deferred at the user's request because this deployment has no public HTTPS endpoint. Local SDK OAuth verification covers the protocol path; it does not establish a connection from ChatGPT's cloud service.

### Follow-up validation

- Full client suite: **706 passed across 110 suites**. The final setup-message change also passed all **14 modal tests** and client TypeScript compilation.
- Full server suite: **388 passed across 58 suites**, including OAuth consent, PKCE/client/callback/resource binding, refresh replay, expiry, role changes, production CSRF discovery, and unsupported-HTTP-origin compatibility. Server TypeScript compilation and diff checks passed.
- Final production-style images compiled both real Vite bundles and the backend successfully. Only `gravity-test-prod` frontend/backend were recreated; both are healthy and `/api/v1/health` reports production at `http://localhost:9999`. PostgreSQL, Redis, RustFS, and existing volumes were retained.
- Live official SDK OAuth passed through the deployed nginx/backend with real PostgreSQL: metadata discovery, dynamic registration, S256 PKCE, real-session consent, exact granted tool discovery, rejected writes and wrong-workspace access, token refresh, own account inventory, and revocation of access plus renewal. It sent no custom workspace header or browser cookie on MCP calls.
- The existing custom-header REST generation → SDK initialization/read → revoke lifecycle also passed after deployment. Both smoke runs removed their disposable accounts, workspaces, credentials, sessions, and OAuth clients/requests.
- A live PostgreSQL probe with a one-connection pool completed authorization-code exchange, token refresh, and bound access verification without deadlock. The probe exited cleanly and verified zero remaining fixture rows.

Evidence: `/tmp/gravity-mcp-oauth-client-full.log`, `/tmp/gravity-mcp-oauth-server-full-final.log`, `/tmp/gravity-mcp-oauth-build-final.log`, `/tmp/gravity-mcp-oauth-restart.log`, `/tmp/gravity-mcp-oauth-live.log`, `/tmp/gravity-mcp-oauth-manual-regression.log`, and `/tmp/gravity-mcp-oauth-pool-live.log`.

## Published PR #217 review

Reviewed `58985b2d..c3655c37` across OAuth and authorization, ticket/domain operations, client connections and consent, transports, providers, and realtime behavior. Four actionable findings were reproduced and fixed:

| Finding | Fix and regression evidence |
| --- | --- |
| P1: Assignment validation acquired a second database connection while its transaction held the project lock. A full pool could stall assignments and moves. | Membership validation now queries through the active transaction, preserving implicit workspace-owner and team-project membership rules. A real PostgreSQL probe reproduced failure with one connection and success with two before the fix; regression tests reject any global query inside the create/update transaction. |
| P2: Vite matched OAuth routes against the full request URL, so `/authorize?...` missed the proxy and returned the SPA. | OAuth proxy matching accepts query boundaries while retaining exact endpoint names. An isolated real Vite server and stub backend reproduced the failure, then passed authorization, discovery/token, and SPA-route isolation checks. |
| P2: Whitespace-only assignee IDs passed schema validation but normalized to null, allowing `assign_ticket` or `update_ticket` to bypass disabled unassignment. | Identifier schemas require a non-whitespace character. Regressions cover spaces, tabs/newlines, and non-breaking spaces through both tools while preserving valid assignment and explicit null semantics. |
| P2: Project/team scope was read before acquiring the write lock. A concurrent team move could admit an old-team cycle or label and leave the ticket invalid for later edits. | Ticket writes lock projects in deterministic order and verify the current workspace/team against the observed scope before mutation. Stale requests fail with a retry message. Gated regressions cover create, update, move, and additive label writes, followed by successful fresh edits. |

Independent OAuth/account review found no additional actionable issue; 35 tests across seven OAuth, CSRF, availability, inventory, credential, and authorization suites passed. The domain fixes passed 40 focused tests. The Vite regression passed all three actual HTTP tests in an isolated Docker runtime; host Node 26's native Vite toolchain crashes with SIGILL, so no host test was silently skipped.

Final server validation after these fixes: **396 tests passed across 58 suites**. The server production build, client application and Vite-config TypeScript checks, and diff checks passed. Existing full client runtime coverage remains 706 tests; only Vite configuration and its standalone integration test changed during this review. These review fixes do not restart the deployed application containers.

Patched-code validation against real PostgreSQL with a single pooled connection passed implicit-owner assignment, rejection/rollback without required project membership, allowed project-member assignment, and moving an assigned ticket. The subprocess exited cleanly and verified zero remaining fixture rows; the live application was not modified or restarted. Evidence: `/tmp/gravity-pr217-assignee-pool-fixed.log`, `/tmp/gravity-pr217-server-final.log`, `/tmp/gravity-pr217-server-build-final.log`, and `/tmp/gravity-pr217-vite-oauth-green.log`.

No unresolved actionable findings remain from this review. Broader verification limits above still apply.

Tracking: GRAV-212–GRAV-229 record completed branch work. GRAV-230 remains open for actual ChatGPT OAuth verification after a public HTTPS URL is available.
